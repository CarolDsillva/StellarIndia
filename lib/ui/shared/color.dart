import 'dart:ui';

// A utility class for defining custom colors used in the app
class AppColor {
  static final pripmaryColor = Color.fromARGB(255, 22, 25, 40);
  static final secondaryColor = Color.fromARGB(255, 32, 58, 86);
}
